"""Pipelines theo thủ tục (package-by-feature).

Mỗi thủ tục là 1 thư mục con, bên trong tách `auto_fill/` (điền form) và `attach/` (đính kèm)
tùy thủ tục có phần nào. Phần dùng chung đặt ở `_shared/`.
"""
