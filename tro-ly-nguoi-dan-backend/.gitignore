__pycache__/
*.py[cod]
.venv/
venv/
.env
# CHỈ thư mục data/ ở GỐC repo (file upload runtime). KHÔNG được viết trần "data/" —
# nó nuốt luôn app/locations/data/ (JSON xã/phường) làm server thiếu danh sách xã (bug 2026-07-22).
/data/
*.log
.pytest_cache/
.DS_Store
.mypy_cache/
.ruff_cache/
